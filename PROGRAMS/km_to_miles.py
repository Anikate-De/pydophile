# Python Program to Convert Kilometres to Miles

kms = float(input('Enter the number of kilometres - '))

# We use the formula miles = kms / 1.6
print(kms, 'Kilometers converts to', kms / 1.6, 'miles')
